<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.mackenzie.myplaces"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="11" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"></uses-permission>  
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission> 
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
     
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--  Main Activity -->
        <activity
            android:name="MainActivity"
            android:noHistory="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!--  Login Activity -->
        <activity
            android:name="LoginActivity"
            android:noHistory="true"
            android:label="@string/app_name"/>
        

        <!--  Cadastro Activity -->
        <activity
            android:name="CadastroActivity"
            android:noHistory="true"
            android:label="@string/app_name"/>
        
        <!--  ABAS Activity -->
        <activity android:name="TabLayoutActivity" />
            
        <!--  Checkin Activity -->
        <activity android:name="CheckinActivity" />
        
        <!--  Profile Activity -->
        <activity android:name="ProfileActivity" />
        
        <!--  Feed Checkin Activity -->
        <activity android:name="FeedCheckinActivity" />
        
        <!--  Cadastro novo local Checkin Activity -->
        <activity android:name="CheckinCadastroActivity" />
        
        <!--  Busca por usuários Activity -->
        <activity android:name="SearchFriendsActivity" />
        
        
        
        
        
    </application>
    
    

</manifest>
